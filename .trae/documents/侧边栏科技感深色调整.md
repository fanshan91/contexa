## 目标（按你要求“使用#1890FF”）
- 不直接用纯 `#1890FF` 做整块侧栏底色（会太亮），而是**以 #1890FF 作为基色进行加深**，得到“科技感深蓝黑”的侧栏背景。

## 推荐侧栏底色（由 #1890FF 派生）
- **推荐：#062440**（RGB: `6 36 64`）
  - 计算方式：`#1890FF` 乘以约 25%（加深保留色相），既有科技蓝倾向，又足够沉稳。
- 如果你想更亮一点：#072A4D（RGB `7 42 77`，约 30%）
- 如果你想更黑一点：#041A2E（RGB `4 26 46`，约 18%）

## 我会怎么改（确认后直接落地）
1. 修改 [globals.css:L49](file:///Users/victor/Desktop/%E9%A1%B9%E7%9B%AE/TMS/contexa/app/globals.css#L49) 的 `--sidebar`：
   - 从当前 `11 31 58`（#0B1F3A）改为 **`6 36 64`（#062440，来自 #1890FF）**。
2. 同步修改 `.dark` 下的 `--sidebar` 为相同值（避免未来启用暗色模式时不一致）。
3. 不改你现有交互策略：hover/active 仍然是白色叠加（10%/15%），选中指示条仍用 `primary`（#1890FF）保证品牌识别。
4. 本地预览检查侧边栏观感与 hover/active 对比。

这样既满足“使用 #1890FF（作为基色）”，又能得到更高级的科技感深色侧边栏。