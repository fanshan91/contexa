## 现状问题
- 你把 `primary` 改为 `#1890FF` 后，目前 `sidebar` 直接跟随 `primary`，导致左侧菜单整块背景过亮、抢视觉焦点。

## 建议的侧边栏底色（更适合“后台/飞书钉钉风格”）
- **默认推荐（深蓝灰，稳重但有品牌蓝倾向）**：`#0B1F3A`（RGB: `11 31 58`）
- 备选 1（更贴近主色调一点） ：`#0A2C53`（RGB: `10 44 83`）
- 备选 2（更偏飞书/钉钉的偏黑侧栏） ：`#0F172A`（RGB: `15 23 42`）

## 具体落地改法（确认后我会直接改代码）
1. 在 [globals.css](file:///Users/victor/Desktop/%E9%A1%B9%E7%9B%AE/TMS/contexa/app/globals.css) 把 `--sidebar` 从 `var(--primary)` 改为上面推荐色（默认用 `#0B1F3A`）。
   - `--sidebar-foreground` 继续用白色（`var(--primary-foreground)`）
   - `--sidebar-accent` 继续保持白色，用于 hover/active 的半透明叠加（现在的 `hover:bg-sidebar-accent/10` 与 `bg-sidebar-accent/15` 不用改）
2. 为了保证品牌识别，**把选中项左侧指示条恢复为主色 `primary`**（现在是白色）：
   - Dashboard 菜单：改 [layout.tsx:L29-L35](file:///Users/victor/Desktop/%E9%A1%B9%E7%9B%AE/TMS/contexa/app/(dashboard)/dashboard/layout.tsx#L29-L35) 中 `before:bg-sidebar-accent-foreground/90` → `before:bg-primary`
   - Project 菜单：改 [project-sidebar.tsx:L31-L38](file:///Users/victor/Desktop/%E9%A1%B9%E7%9B%AE/TMS/contexa/app/(dashboard)/projects/%5BprojectId%5D/project-sidebar.tsx#L31-L38) 同样替换
3. 本地预览快速验收：侧边栏底色不刺眼、hover/active 仍有明显层级、选中指示条品牌蓝清晰可见。

## 验收标准
- 侧边栏整体不再“亮蓝一整块”，观感更沉稳
- hover（10%）与 active（15%）在深底上仍明显
- 选中项左侧指示条为主色蓝，识别强且不抢主区域内容

确认后我会按“默认推荐 #0B1F3A”直接落地；如果你更偏好备选色，我也可以按你选的那一个执行。